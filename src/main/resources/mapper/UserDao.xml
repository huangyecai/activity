<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyc.activity.modules.user.dao.UserDao">
	<select id="get" resultType="com.hyc.activity.modules.user.entity.User">
		select
		*
		from test
		where id = 1
	</select>
</mapper>